# Port the API listens on
PORT=4000

# Comma separated list of allowed origins for CORS (omit to allow all)
CORS_ORIGIN=http://localhost:3000

# Destination for contact emails
CONTACT_TO=ryan@rjchicago.com
CONTACT_FROM=web@rjchicago.com

# SMTP configuration (used by Nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=supersecret
SMTP_SECURE=false

# If SMTP settings are not provided, emails will log to the console.
